server:
  port: 9004

spring:
  application:
    name: cloud-alibaba-provider-payment-sentinel
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

management:
  endpoints:
    web:
      exposure:
        include: "*"